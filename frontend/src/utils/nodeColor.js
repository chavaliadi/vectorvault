/**
 * Utility to map nodes to their active traversal state colors.
 * Placeholder for Phase 4A.
 */

/**
 * Returns a default color for nodes.
 * @param {object} node
 * @param {object|null} currentStep
 * @param {Array} hnswResults
 * @returns {string}
 */
export function getNodeColor(node, currentStep, hnswResults = []) {
  // Returns default slate-blue color during scaffolding phase
  return "#1E293B";
}
